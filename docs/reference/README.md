# Reference

Upstream documentation mirrored here for offline use. **We do not author these
files.** Fix an error upstream first; a local edit silently diverges from the
source it claims to mirror.

| Mirror | Upstream |
|---|---|
| [kiro-cli/](kiro-cli/README.md) | The `kiro-cli` documentation. Its ACP surface, agent-spec schema, and MCP config shape are load-bearing when `agent.provider=acp`. |

Where Kiro Crew's own behavior differs from a mirrored page, Kiro Crew's docs win:
this fork supports KiroACP and Codex ACP and does not use every capability the
upstream Kiro CLI documents.
